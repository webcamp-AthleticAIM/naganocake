  <div class='d-flex align-content-between flex-wrap'>
    <% items.each do |item| %>
      <div class='mr-3'>
        <%= attachment_image_tag item, :image, format: 'jpg', size: "180x100" %><br>
        <%= item.name %><br>
        ￥<%= item.add_tax_price.to_s(:delimited) %>
      </div>
    <% end %>
  </div>