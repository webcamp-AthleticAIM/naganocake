  <div class='d-flex align-content-between flex-wrap'>
    <% items.each do |item| %>
      <div class='mr-3'>
        <%= link_to item_path(item.id) do %>
        <%= attachment_image_tag item, :image, size: "180x100" %><br>
        <% end %>
        <%= item.name %><br>
        <p class='mb-3'>￥<%= item.add_tax_price.to_s(:delimited) %></p>
      </div>
    <% end %>
  </div>